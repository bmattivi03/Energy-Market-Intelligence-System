# ENTSO-E Transparency Platform API token.
# Register at https://transparency.entsoe.eu/ then request Web API access in your
# account settings. Required ONLY to re-run ingestion (src/ingestion/run_ingestion.py);
# the shipped splits/checkpoints already let you run training and evaluation.
ENTSOE_TOKEN=your_token_here
