# Python
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# OS / editors
.DS_Store
.idea/
.vscode/
.venv/
venv/

# Run artifacts and caches
catboost_info/
.cache.sqlite
*.log

# Secrets (use .env.example as the template)
.env

# NOTE: data/, checkpoints/, and *.parquet are intentionally tracked so the
# shipped artifacts reproduce the headline results without re-running ingestion.
